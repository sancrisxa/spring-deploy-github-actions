name: Deploy Application Youtube

on:
    push:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Setup Java
              uses: actions/setup-java@v3
              with:
                distribution: 'temurin'
                java-version: '17'
            - name: Build project
              run: mvn clean install -DskipTests
            - name: Login Docker Hub
              run: docker login -u sancrisxa -p san1cris2xa3
            - name: Build docker image
              run: docker build -t sancrisxa/deploy_ghactions .
            - name: Push image docker
              run: docker push sancrisxa/deploy_ghactions
            